#include "main.h"

/**
 * int mul - multiplies two integers
 * @a: int to be multiplied to b
 * @b: int to be multiplied to a
 * @result: results for a * b
 * Return; returns results
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
