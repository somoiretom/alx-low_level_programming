#include "main.h"
/**
 * _puts - outputs a string
 * @str: pointer to print
 * Return: return void
 */


void _puts(char *str)
{
	int x = 0;

	while (str[x)
	{
		_putchar(str[x]);
		x++;
	}

	_putchar('\n');
}
