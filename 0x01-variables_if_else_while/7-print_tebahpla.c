#include <stdio.h>

/**
 * main - main code block
 *
 * Return: returns zero integer data type
 */

int main(void)
{
	char alphabet;

	for (alphabet = 'z'; alphabet >= 'a'; alphabet--)

	putchar(alphabet);
	putchar('\n');

	return (0);
}
