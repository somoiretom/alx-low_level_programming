#include <stdio.h>

/**
 * main - main code block
 *
 * Return: returns integer zero
 */

int main(void)
{
	int digit;

	for (digit = 0; digit < 10; digit++)

	putchar(digit + '0');
	putchar('\n');

	return (0);
}
